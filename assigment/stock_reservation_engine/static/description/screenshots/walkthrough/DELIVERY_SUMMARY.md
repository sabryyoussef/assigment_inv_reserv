# Walkthrough capture summary

Regenerate with:

```powershell
cd tools\screenshots
npm run walkthrough
```

Outputs appear in this folder (`static/description/screenshots/walkthrough/`). Machine-readable status: `CAPTURE_SUMMARY.json` when the script completes.
